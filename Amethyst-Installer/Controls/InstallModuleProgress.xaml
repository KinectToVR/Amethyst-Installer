<UserControl x:Class="amethyst_installer_gui.Controls.InstallModuleProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:amethyst_installer_gui.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Border Background="{StaticResource BrushBackground}" CornerRadius="4">
        <StackPanel Orientation="Vertical">
            <Grid x:Name="heading">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBlock x:Name="moduleTitle" Foreground="White" FontFamily="Segoe UI Semibold" FontWeight="SemiBold" Text="When the impostor is among us" Grid.Column="0" />
                <local:ProgressSpinner Grid.Column="1" />
            </Grid>

            <RichTextBox x:Name="detailedLog"></RichTextBox>
        </StackPanel>
    </Border>
</UserControl>
